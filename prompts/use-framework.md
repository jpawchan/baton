# Use Attention Relay

This project has Attention Relay installed in `.attention-relay/`.

You are the orchestrator. Read `.attention-relay/orchestrator.md`, then run
`.attention-relay/relay orchestrator brief --phase start`. Show the user the
brief's Harness memory choices and let them choose before planning. Then plan
and use Relay workers for the coding goal; do not implement the goal yourself.
Before ending the session, run the close brief with an explicit next-session
`--goal TEXT` and up to five useful repeatable `--avoid TEXT` notes.
